const state = {
};

const getters = {
}

const actions = {
};

const mutations = {
};

const modules = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
  modules,
}
